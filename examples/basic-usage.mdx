---
title: "Basic Usage Examples"
description: "Practical examples and common workflows for getting started with Qwen CLI"
---


## Getting Started Examples

### Your First Code Generation

<Steps>
  <Step title="Simple Function">
    ```bash
    qwen "Create a JavaScript function to validate email addresses"
    ```
    
    **Output:**
    ```javascript
    function validateEmail(email) {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      
      if (!email || typeof email !== 'string') {
        return false;
      }
      
      return emailRegex.test(email.toLowerCase());
    }
    
    // Usage example:
    console.log(validateEmail("user@example.com")); // true
    console.log(validateEmail("invalid.email")); // false
    ```
  </Step>
  
  <Step title="API Endpoint">
    ```bash
    qwen "Create Express.js endpoint for user registration with validation"
    ```
    
    **Output:**
    ```javascript
    const express = require('express');
    const bcrypt = require('bcrypt');
    const router = express.Router();
    
    // User registration endpoint
    router.post('/register', async (req, res) => {
      try {
        const { email, password, name } = req.body;
        
        // Input validation
        if (!email || !password || !name) {
          return res.status(400).json({
            error: 'Missing required fields: email, password, name'
          });
        }
        
        // Email validation
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
          return res.status(400).json({
            error: 'Invalid email format'
          });
        }
        
        // Password validation
        if (password.length < 8) {
          return res.status(400).json({
            error: 'Password must be at least 8 characters long'
          });
        }
        
        // Hash password
        const saltRounds = 12;
        const hashedPassword = await bcrypt.hash(password, saltRounds);
        
        // TODO: Save user to database
        const user = {
          id: Date.now(),
          email,
          name,
          password: hashedPassword,
          createdAt: new Date()
        };
        
        res.status(201).json({
          message: 'User created successfully',
          user: {
            id: user.id,
            email: user.email,
            name: user.name,
            createdAt: user.createdAt
          }
        });
      } catch (error) {
        console.error('Registration error:', error);
        res.status(500).json({
          error: 'Internal server error'
        });
      }
    });
    
    module.exports = router;
    ```
  </Step>
  
  <Step title="React Component">
    ```bash
    qwen "Create a React component for a product card with image, title, price, and add to cart button"
    ```
    
    **Output:**
    ```tsx
    import React from 'react';
    
    interface Product {
      id: string;
      title: string;
      price: number;
      image: string;
      description?: string;
    }
    
    interface ProductCardProps {
      product: Product;
      onAddToCart?: (product: Product) => void;
      loading?: boolean;
    }
    
    const ProductCard: React.FC<ProductCardProps> = ({ 
      product, 
      onAddToCart,
      loading = false 
    }) => {
      const handleAddToCart = () => {
        if (onAddToCart) {
          onAddToCart(product);
        }
      };
    
      return (
        <div className="product-card bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
          <div className="aspect-w-1 aspect-h-1">
            <img 
              src={product.image} 
              alt={product.title}
              className="w-full h-48 object-cover"
              loading="lazy"
            />
          </div>
          
          <div className="p-4">
            <h3 className="text-lg font-semibold text-gray-900 mb-2 line-clamp-2">
              {product.title}
            </h3>
            
            <p className="text-2xl font-bold text-blue-600 mb-4">
              ${product.price.toFixed(2)}
            </p>
            
            <button
              onClick={handleAddToCart}
              disabled={loading}
              className="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"
            >
              {loading ? 'Adding...' : 'Add to Cart'}
            </button>
          </div>
        </div>
      );
    };
    
    export default ProductCard;
    ```
  </Step>
</Steps>

## Common Development Tasks

### Database Operations

<Tabs>
  <Tab title="SQL Queries">
    ```bash
    # Create database schema
    qwen "Create SQL schema for e-commerce database with users, products, and orders"
    
    # Complex query
    qwen "Write SQL query to get top 10 customers by total order value in the last 6 months"
    
    # Database optimization
    qwen "Optimize this slow SQL query" --query "SELECT * FROM orders WHERE created_at > '2023-01-01'"
    ```
  </Tab>
  
  <Tab title="ORM Models">
    ```bash
    # Prisma schema
    qwen "Create Prisma schema for blog application with users, posts, and comments"
    
    # Mongoose models
    qwen "Create Mongoose models for task management app with users, projects, and tasks"
    
    # Sequelize models
    qwen "Create Sequelize models with associations for social media platform"
    ```
  </Tab>
  
  <Tab title="Database Migrations">
    ```bash
    # Create migration
    qwen "Create database migration to add email verification to users table"
    
    # Data migration
    qwen "Write migration script to update existing user records with default values"
    ```
  </Tab>
</Tabs>

### Testing

<CodeGroup>
```bash Unit Tests
# Jest tests
qwen "Write Jest unit tests for this utility function" --file ./utils/formatDate.js

# React component tests
qwen "Create React Testing Library tests for LoginForm component" --file ./components/LoginForm.tsx
```

```bash Integration Tests
# API tests
qwen "Write integration tests for user authentication API" --file ./routes/auth.js

# Database tests
qwen "Create tests for database operations in user service" --file ./services/userService.js
```

```bash E2E Tests
# Playwright tests
qwen "Write Playwright tests for user registration flow"

# Cypress tests
qwen "Create Cypress tests for shopping cart functionality"
```
</CodeGroup>

### Documentation

<AccordionGroup>
  <Accordion title="API Documentation">
    ```bash
    # Generate OpenAPI spec
    qwen "Create OpenAPI specification for this Express.js API" --file ./routes/
    
    # README for API
    qwen "Write comprehensive README for REST API with authentication examples"
    
    # JSDoc comments
    qwen "Add JSDoc comments to all functions" --file ./utils/helpers.js
    ```
  </Accordion>
  
  <Accordion title="Code Documentation">
    ```bash
    # Function documentation
    qwen "Add comprehensive documentation to this complex function" --file ./algorithms/pathfinding.js
    
    # Class documentation
    qwen "Document this TypeScript class with all methods and properties" --file ./models/User.ts
    
    # Component documentation
    qwen "Create Storybook stories for this React component" --file ./components/Button.tsx
    ```
  </Accordion>
</AccordionGroup>

## File Operations

### Working with Existing Files

<CodeGroup>
```bash Analyze Files
# Code analysis
qwen analyze ./src/components/Dashboard.tsx

# Performance review
qwen performance ./utils/dataProcessor.js

# Security audit
qwen security ./api/routes/auth.js
```

```bash Modify Files
# Add features
qwen "Add error handling to this function" --file ./services/apiClient.js

# Refactor code
qwen "Convert this class component to hooks" --file ./components/UserProfile.jsx

# Add types
qwen "Add TypeScript types to this JavaScript module" --file ./utils/helpers.js
```

```bash Fix Issues
# Debug errors
qwen "Fix the TypeError in this React component" --file ./components/ProductList.tsx

# Resolve conflicts
qwen "Fix merge conflicts in this file" --file ./src/App.js

# Update dependencies
qwen "Update this code to work with React 18" --file ./components/LegacyComponent.jsx
```
</CodeGroup>

## Configuration Examples

### Project Setup

<Tabs>
  <Tab title="Package Configuration">
    ```bash
    # package.json setup
    qwen "Create package.json for React TypeScript project with common dev dependencies"
    
    # webpack config
    qwen "Create webpack configuration for React app with TypeScript and hot reload"
    
    # ESLint config
    qwen "Create ESLint configuration for TypeScript React project with Prettier"
    ```
  </Tab>
  
  <Tab title="Environment Setup">
    ```bash
    # Docker setup
    qwen "Create Dockerfile for Node.js application with multi-stage build"
    
    # Docker Compose
    qwen "Create docker-compose.yml for full-stack app with React, Node.js, and PostgreSQL"
    
    # CI/CD
    qwen "Create GitHub Actions workflow for testing and deployment"
    ```
  </Tab>
  
  <Tab title="Database Setup">
    ```bash
    # Database initialization
    qwen "Create database setup script with initial tables and seed data"
    
    # Environment config
    qwen "Create database configuration for development, staging, and production"
    ```
  </Tab>
</Tabs>

## Quick Fixes and Utilities

### Common Bug Fixes

<CardGroup cols={1}>
  <Card title="React Errors" icon="react">
    ```bash
    qwen "Fix 'Cannot read property of undefined' error"
    qwen "Resolve React hook dependency warnings"
    qwen "Fix infinite render loop in useEffect"
    ```
  </Card>
  
  <Card title="JavaScript Issues" icon="js">
    ```bash
    qwen "Fix async/await error handling"
    qwen "Resolve Promise chain issues"
    qwen "Fix scope and closure problems"
    ```
  </Card>
  
  <Card title="Node.js Problems" icon="node-js">
    ```bash
    qwen "Fix Express.js middleware order issue"
    qwen "Resolve CORS configuration problems"
    qwen "Fix database connection timeout"
    ```
  </Card>
  
  <Card title="TypeScript Errors" icon="typescript">
    ```bash
    qwen "Fix TypeScript type inference issues"
    qwen "Resolve generic type constraints"
    qwen "Fix module import/export errors"
    ```
  </Card>
</CardGroup>

### Utility Functions

<AccordionGroup>
  <Accordion title="Data Manipulation">
    ```bash
    # Array operations
    qwen "Create utility functions for array operations: chunk, unique, groupBy"
    
    # Object utilities
    qwen "Write functions for deep object manipulation: merge, clone, pick"
    
    # String utilities
    qwen "Create string utility functions: slugify, truncate, capitalize"
    ```
    
    **Example output:**
    ```javascript
    // Array utilities
    const chunk = (array, size) => {
      return array.reduce((chunks, item, index) => {
        const chunkIndex = Math.floor(index / size);
        if (!chunks[chunkIndex]) {
          chunks[chunkIndex] = [];
        }
        chunks[chunkIndex].push(item);
        return chunks;
      }, []);
    };
    
    const unique = (array) => {
      return [...new Set(array)];
    };
    
    const groupBy = (array, key) => {
      return array.reduce((groups, item) => {
        const group = item[key];
        groups[group] = groups[group] || [];
        groups[group].push(item);
        return groups;
      }, {});
    };
    ```
  </Accordion>
  
  <Accordion title="API Helpers">
    ```bash
    # HTTP client
    qwen "Create HTTP client with error handling, retries, and request/response interceptors"
    
    # API utilities
    qwen "Write utilities for API response handling: success, error, loading states"
    
    # Authentication helpers
    qwen "Create JWT token management utilities: encode, decode, refresh"
    ```
  </Accordion>
</AccordionGroup>

## Interactive Examples

### Using Interactive Mode

```bash
# Start interactive session
qwen interactive

# Example conversation:
> "Create a login form component"
> "Add form validation to the previous component"
> "Now add TypeScript types"
> "Create unit tests for this component"
```

### Context-Aware Development

<Steps>
  <Step title="Set Project Context">
    ```bash
    # Initialize project context
    qwen init --scan-project
    ```
  </Step>
  
  <Step title="Use Context in Requests">
    ```bash
    # AI understands your project structure
    qwen "Add authentication to this app"
    qwen "Create a new feature that integrates with existing user system"
    ```
  </Step>
  
  <Step title="Maintain Consistency">
    ```bash
    # Follow existing patterns
    qwen "Create another API endpoint following the same pattern as users endpoint"
    ```
  </Step>
</Steps>




<Tip>
**Pro Tip**: Start with simple requests and gradually add more requirements as you get comfortable with Qwen CLI. The AI learns from your patterns and provides better results over time.
</Tip>